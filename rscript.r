#!/usr/bin/env Rscript

# Print a message
print("Hello from R!")

# Perform a simple calculation
result <- 10 + 5
print(paste("The result of 10 + 5 is:", result))

# Create a simple vector and calculate its mean
numbers <- c(1, 2, 3, 4, 5)
mean_value <- mean(numbers)
print(paste("The mean of numbers 1-5 is:", mean_value))